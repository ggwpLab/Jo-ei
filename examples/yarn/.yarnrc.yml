# Yarn Berry (v2+): put in the project root.
# Yarn Classic (v1) instead uses:  yarn config set registry http://localhost:8080/yarn/
npmRegistryServer: "http://localhost:8080/yarn/"

# Yarn Berry refuses plain HTTP unless the host is whitelisted:
unsafeHttpWhitelist:
  - localhost
